const staticDirBasePath = 'payload-media';

export const collectionGroup = {
  layout: 'Layout',
  list: 'CMS',
  media: 'Media / Assets',
  admin: 'System'
};

export const staticDir = {
  static: `${staticDirBasePath}/static`,
  responsive: `${staticDirBasePath}/responsive`,
  renderPasses: `${staticDirBasePath}/passes`
};
